{
  "name": "finvision-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "FinVision Financial Modeling Platform - Monorepo",
  "scripts": {
    "dev": "cd frontend && pnpm dev",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm run test:minimal:ci",
    "lint": "cd frontend && npm run lint:ci"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
