{
  "name": "finvision-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "npm@10.9.3",
  "description": "FinVision is a comprehensive web-based financial modeling and analysis platform that transforms static Excel financial models into dynamic, interactive dashboards with real-time parameter adjustment capabilities.",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:tokens": "style-dictionary build --config config/style-dictionary.json && cp dist/tokens/json/tokens.json frontend/src/design-system/tokens.json && npm --prefix frontend run gen:tokens && npx --prefix frontend tsx scripts/generate-tailwind-config.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "style-dictionary": "^4.4.0"
  }
}
